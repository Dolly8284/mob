<div id="<%= dom_id post %>"
  <h2><%= @post.title %></h2>

  <% if @post.avatar.attached? %>
  <h6><%= image_tag @post.avatar, width: "300px" %></h6>
  <% end %>

  <h5>Posted-By-<%= post.user.email %></h5>

  <h6><%= pluralize(post.views, "view") %></h6>

  <div>
    <%= post.body %>
  </div>